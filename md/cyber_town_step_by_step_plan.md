# 赛博小镇：逐步学习与项目复现计划书

## 1. 项目目标

通过“边学边做、边做边学”的方式，从零实现一个由大语言模型驱动的 2D AI 小镇。最终系统应具备：

- 多个具有独立身份和性格的智能 NPC；
- 工作记忆与长期语义记忆；
- 好感度及关系等级变化；
- NPC 状态管理和背景对话；
- FastAPI 后端接口与日志系统；
- Godot 2D 场景、玩家移动、NPC 交互和对话 UI；
- Godot 与 Python 后端的异步 HTTP 通信；
- 可以完整运行、测试和展示的最终项目。

## 2. 当前进度

当前整体完成度约为 **25%**。

已经完成：

- DeepSeek API 配置读取与调用；
- 单 NPC 的身份、性格和系统提示词；
- `NPC` 类封装；
- 最近 5 轮工作记忆；
- 完整对话 JSON 档案；
- Embedding 语义向量生成；
- JSON 向量长期记忆和相似度检索；
- 旧对话向量迁移；
- `config.py`、`memory.py`、`npc.py`、`main.py` 模块拆分；
- 使用 `npc_id` 为不同 NPC 准备独立记忆文件。

当前项目结构：

```text
15-cyber-town-from-scratch/
├── main.py
├── config.py
├── npc.py
├── memory.py
├── data/
├── .env
└── pyproject.toml
```

---

## 3. 后续逐步实施计划

## 第 1 步：验证独立 NPC 记忆

### 学习内容

- 唯一标识 `npc_id` 的作用；
- 数据隔离的概念；
- 为什么不同 NPC 不能共用同一份工作记忆。

### 实施内容

- 确认林舟使用 `npc_id="lin_zhou"`；
- 确认记忆文件路径由 `npc_id` 动态生成；
- 验证林舟仍能读取原有普通记忆和向量记忆。

### 验收标准

- 程序能够正常启动；
- 林舟能够读取旧记忆；
- 数据文件仍为 `lin_zhou_memory.json` 和 `lin_zhou_vector_memory.json`；
- 没有路径、参数或导入错误。

### 完成后预计总进度

**27%**

---

## 第 2 步：实现多个 NPC

### 学习内容

- 类的多实例机制；
- 同一个 `NPC` 类如何产生不同角色；
- 配置数据与业务代码分离。

### 实施内容

- 创建林舟、王教授和康康三个 NPC；
- 给每个 NPC 配置不同的姓名、职业、性格和知识范围；
- 为每个 NPC 设置不同的 `npc_id`；
- 将 NPC 配置集中保存，避免在代码中重复创建。

### 验收标准

- 三个 NPC 都能独立回复；
- 三个 NPC 的说话风格明显不同；
- 每个 NPC 生成自己的普通记忆和向量记忆文件；
- 林舟不会读取王教授的私人对话，王教授也不会读取林舟的私人对话。

### 完成后预计总进度

**30%**

---

## 第 3 步：实现 `NPCAgentManager`

### 学习内容

- 管理器模式；
- 对象注册、查找和统一管理；
- 为什么主程序不应直接维护大量 NPC 对象。

### 实施内容

- 新建 `agents.py`；
- 创建 `NPCAgentManager`；
- 实现 NPC 初始化、注册、查询和列表获取；
- 在终端中实现 NPC 选择与切换；
- 输入“返回”时结束当前 NPC 对话并重新选择居民。

### 验收标准

- 玩家能查看全部 NPC；
- 玩家能选择任意 NPC 对话；
- 切换 NPC 后角色和记忆正确变化；
- 输入不存在的 NPC 时能够得到明确提示。

### 完成后预计总进度

**34%**

---

## 第 4 步：完善记忆质量

### 学习内容

- 原始对话、工作记忆和长期事实的区别；
- 记忆污染、重复记忆和冲突记忆；
- 相似度阈值与 Top-K 检索；
- 结构化记忆的重要性。

### 实施内容

- 避免把问题和未完成句子保存为长期事实；
- 对完全重复的事实去重；
- 给长期记忆增加时间、来源和 NPC 标识；
- 处理“我以前喜欢 A，现在喜欢 B”这类冲突；
- 编写固定问题测试语义检索效果；
- 为后续 SQLite/Qdrant 存储保留统一接口。

### 验收标准

- 普通问题不会被误存为个人事实；
- 重复陈述不会无限增加向量记录；
- 新事实能够替代或标记旧事实；
- 相关问题能检索到正确事实，无关问题不会频繁误检索。

### 完成后预计总进度

**38%**

---

## 第 5 步：实现批量背景对话

### 学习内容

- 即时响应与批量生成的区别；
- 使用一次 LLM 调用生成多个结果；
- JSON 格式输出和异常解析；
- 缓存与成本控制。

### 实施内容

- 创建 `NPCBatchGenerator`；
- 一次调用生成全部 NPC 的背景台词或活动描述；
- 让背景台词符合时间、地点、职业和性格；
- 解析模型返回的 JSON；
- 区分“NPC 背景对话”和“玩家即时对话”。

### 验收标准

- 一次 API 调用能生成三个 NPC 的背景台词；
- 返回结果可以稳定解析为字典；
- 每个 NPC 的台词符合角色设定；
- JSON 解析失败时不会导致程序直接崩溃。

### 完成后预计总进度

**42%**

---

## 第 6 步：实现基础好感度系统

### 学习内容

- 状态持久化；
- 情感分析；
- 分数裁剪；
- 数值状态与等级状态的映射。

### 实施内容

- 新建 `relationship_manager.py`；
- 保存每名 NPC 对玩家的好感度；
- 根据玩家消息判断友好、中立或不友好；
- 将每次变化限制在合理范围；
- 建立陌生、熟悉、友好、亲密、挚友五个等级；
- 把好感度保存到 JSON 或 SQLite。

### 验收标准

- 友好消息能够增加好感度；
- 不友好消息能够降低好感度；
- 分数始终保持在 0～100；
- 重启程序后好感度仍然存在；
- 不同 NPC 的好感度相互独立。

### 完成后预计总进度

**47%**

---

## 第 7 步：让好感度影响 NPC 回复

### 学习内容

- 动态系统提示词；
- 状态如何改变智能体行为；
- 角色一致性和行为约束。

### 实施内容

- 在每次对话前读取当前好感度等级；
- 将关系等级写入 NPC 系统提示词；
- 为五个等级设置不同的回复风格；
- 避免 NPC 因好感度变化而丢失原有职业和性格。

### 验收标准

- 陌生阶段回复礼貌但有距离；
- 好感度提高后回复逐渐自然和热情；
- 关系变化能够被玩家明显感知；
- NPC 始终保持原有身份和性格。

### 完成后预计总进度

**50%**

---

## 第 8 步：建立 FastAPI 后端骨架

### 学习内容

- 前后端分离；
- HTTP 请求和响应；
- FastAPI 路由；
- Pydantic 数据模型；
- CORS 的作用。

### 实施内容

- 安装 FastAPI 和 Uvicorn；
- 将当前 Python 代码整理到 `backend/`；
- 新建 FastAPI 应用；
- 配置 CORS；
- 创建根路由和健康检查接口；
- 在启动阶段初始化 NPC、Embedding 和管理器。

### 验收标准

- 后端可以通过 Uvicorn 启动；
- 浏览器能够访问根路由；
- `/docs` 能显示自动生成的 API 文档；
- NPC 和模型只在服务启动时初始化一次。

### 完成后预计总进度

**56%**

---

## 第 9 步：实现后端数据模型和对话接口

### 学习内容

- 请求模型与响应模型；
- 参数校验；
- HTTP 状态码；
- 异常处理。

### 实施内容

- 新建 `models.py`；
- 定义 `DialogueRequest` 和 `DialogueResponse`；
- 实现 `POST /dialogue`；
- 实现 NPC 列表和单个 NPC 查询接口；
- 实现好感度查询接口；
- 对不存在的 NPC 返回 404。

### 验收标准

- 能在 `/docs` 中直接测试对话；
- 请求不同 `npc_id` 时返回对应 NPC 的回复；
- API 返回回复、好感度和关系等级；
- 无效输入能够返回清晰错误信息。

### 完成后预计总进度

**62%**

---

## 第 10 步：实现 NPC 状态管理

### 学习内容

- 服务端状态；
- 并发请求冲突；
- NPC 忙碌状态和动作状态；
- 时间信息记录。

### 实施内容

- 新建 `state_manager.py`；
- 保存 NPC 的位置、动作、忙碌状态和最后互动时间；
- 对话开始时设置忙碌；
- 对话结束或异常时释放忙碌状态；
- 实现全部 NPC 状态查询接口。

### 验收标准

- 能查询所有 NPC 的状态；
- 对话过程中 NPC 被标记为忙碌；
- 请求结束后 NPC 恢复空闲；
- 异常情况下也能正确释放状态。

### 完成后预计总进度

**66%**

---

## 第 11 步：实现实时日志系统

### 学习内容

- Python `logging`；
- 日志等级；
- 控制台与文件双输出；
- 按日期保存日志。

### 实施内容

- 新建 `logger.py`；
- 记录玩家消息、NPC 回复、长期记忆检索结果；
- 记录好感度变化、变化原因和互动次数；
- 记录后端异常；
- 新建日志查看脚本。

### 验收标准

- 控制台能够实时看到关键日志；
- `logs/` 中生成按日期命名的文件；
- 程序重启不会覆盖当天已有日志；
- 对话错误能够记录堆栈或错误原因。

### 完成后预计总进度

**70%**

---

## 第 12 步：学习 Godot 场景和节点

### 学习内容

- Godot 项目、场景和节点；
- 父子节点关系；
- 场景实例化；
- GDScript 基础语法；
- 信号机制。

### 实施内容

- 安装并打开 Godot；
- 创建游戏项目；
- 创建 `Main`、`Player`、`NPC` 和 `DialogueUI` 四个基础场景；
- 理解每个场景的节点树；
- 设置主场景并运行空白项目。

### 验收标准

- Godot 项目可以正常运行；
- 四个场景均已创建；
- 能解释节点、场景、实例和信号的区别；
- 主场景可以成功实例化其他场景。

### 完成后预计总进度

**74%**

---

## 第 13 步：实现玩家移动与碰撞

### 学习内容

- `CharacterBody2D`；
- 输入映射；
- 速度向量；
- `move_and_slide()`；
- 碰撞体和方向动画。

### 实施内容

- 建立 Player 节点树；
- 实现 WASD 和方向键移动；
- 实现墙体碰撞；
- 添加摄像机跟随；
- 根据移动方向切换动画；
- 对话期间禁止玩家移动。

### 验收标准

- 玩家能够流畅移动；
- 玩家不能穿过墙体；
- 相机能够正确跟随；
- 静止和移动动画可以正常切换。

### 完成后预计总进度

**79%**

---

## 第 14 步：实现 Godot NPC 场景

### 学习内容

- 通用 NPC 场景模板；
- `Area2D` 交互检测；
- 导出变量；
- 随机巡逻与状态切换。

### 实施内容

- 创建通用 `NPC.tscn`；
- 实例化三个 NPC；
- 为实例设置不同名称、职业、外观和位置；
- 检测玩家进入或离开交互范围；
- 实现 NPC 随机巡逻；
- 对话期间停止移动。

### 验收标准

- 三个 NPC 能使用同一个场景模板；
- 玩家靠近时可以触发交互提示；
- NPC 能在限定范围巡逻；
- NPC 不会在对话时继续移动。

### 完成后预计总进度

**84%**

---

## 第 15 步：实现对话 UI

### 学习内容

- `CanvasLayer`；
- `Panel`、`Label`、`RichTextLabel` 和 `LineEdit`；
- 按钮信号；
- UI 状态控制。

### 实施内容

- 创建 `DialogueUI.tscn`；
- 显示 NPC 姓名、职业、玩家消息和 NPC 回复；
- 实现发送、回车发送和关闭；
- 等待回复时禁用重复提交；
- 对话开启时暂停玩家移动。

### 验收标准

- 玩家能够打开和关闭对话框；
- 输入内容能够正确显示；
- 等待响应时不能重复发送；
- NPC 回复能够追加到对话区域。

### 完成后预计总进度

**88%**

---

## 第 16 步：实现 Godot API 客户端

### 学习内容

- `HTTPRequest`；
- 异步网络请求；
- JSON 序列化与反序列化；
- HTTP 响应码；
- AutoLoad 单例和信号。

### 实施内容

- 新建 `api_client.gd`；
- 配置后端 API 地址；
- 实现对话、NPC 状态和 NPC 列表请求；
- 使用信号返回成功结果或错误信息；
- 为不同请求使用独立的 `HTTPRequest` 节点。

### 验收标准

- Godot 能成功请求 FastAPI；
- 中文消息能够正确发送和接收；
- 网络异常时 UI 不会卡死；
- 后端错误能在前端显示明确提示。

### 完成后预计总进度

**93%**

---

## 第 17 步：整合主场景和背景状态

### 学习内容

- 多场景协调；
- 定时器；
- 后端状态轮询；
- 信号驱动的低耦合通信。

### 实施内容

- 在 Main 中实例化玩家、NPC 和对话 UI；
- 定期从后端获取 NPC 状态；
- 更新 NPC 头顶背景台词；
- 接通玩家交互、对话 UI 和 API 客户端；
- 添加基础背景、墙体和必要素材。

### 验收标准

- 玩家能在场景中靠近任意 NPC；
- 按 E 键可以开始对话；
- 回复来自对应的后端 NPC；
- NPC 背景台词可以自动更新；
- 对话关闭后玩家和 NPC 恢复移动。

### 完成后预计总进度

**97%**

---

## 第 18 步：整体联调、测试与整理

### 学习内容

- 端到端测试；
- 错误定位；
- 边界情况；
- 项目文档和演示准备。

### 实施内容

- 测试多 NPC、独立记忆、好感度和角色一致性；
- 测试后端关闭、网络超时和无效请求；
- 测试中文编码和 JSON 解析；
- 检查日志、记忆和好感度持久化；
- 清理临时代码与测试数据；
- 完善 `.gitignore`、README、运行命令和架构说明；
- 准备最终演示流程。

### 验收标准

- 从启动后端到进入 Godot 游戏的流程清晰；
- 玩家可以移动、选择 NPC 并连续对话；
- NPC 具有独立记忆和不同性格；
- 好感度能够变化并影响回复；
- 日志能够完整记录互动；
- 项目重启后关键数据仍然存在；
- README 可以让其他人独立运行项目。

### 完成后预计总进度

**100%**

---

## 4. 最终项目结构目标

```text
Helloagents-AI-Town/
├── backend/
│   ├── main.py
│   ├── agents.py
│   ├── npc.py
│   ├── memory.py
│   ├── relationship_manager.py
│   ├── state_manager.py
│   ├── logger.py
│   ├── models.py
│   ├── config.py
│   ├── data/
│   ├── logs/
│   └── .env
│
└── helloagents-ai-town/
    ├── project.godot
    ├── scenes/
    │   ├── main.tscn
    │   ├── player.tscn
    │   ├── npc.tscn
    │   └── dialogue_ui.tscn
    ├── scripts/
    │   ├── main.gd
    │   ├── player.gd
    │   ├── npc.gd
    │   ├── dialogue_ui.gd
    │   ├── api_client.gd
    │   └── config.gd
    └── assets/
```

## 5. 实施原则

1. 每次只增加一个核心功能，不同时修改过多模块。
2. 每一步都必须先理解数据流，再编写代码。
3. 每完成一步立即运行，不把错误带入下一步。
4. 新增代码需要适量中文注释，并在文件顶部解释主要变量。
5. 先完成最小可运行版本，再逐步提高准确性和美观度。
6. 普通对话、向量记忆、好感度和日志分别保存，避免职责混乱。
7. API 后端完成并测试后，再开始 Godot 前端通信。
8. 每个阶段结束后更新完成度和项目结构。

## 6. 下一步

从 **第 1 步：验证独立 NPC 记忆** 开始。确认林舟的旧记忆没有丢失后，立即进入 **第 2 步：实现多个 NPC**。
